<Window x:Class="electric_network_editor.NetworkEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:electric_network_editor"
        xmlns:uc="clr-namespace:electric_network_editor.Views"
        mc:Ignorable="d"
        Title="NetworkEditorView" Height="800" Width="1500">
    <Grid>
        <!-- Row Definitions for Top Menu and Main Content -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Row for the Menu bar -->
            <RowDefinition Height="*"/>
            <!-- Row for the main content (sidebar + canvas) -->
        </Grid.RowDefinitions>

        <!-- Include the MenuBar UserControl at the top -->
        <uc:MenuBarView Grid.Row="0"/>

        <!-- Main Content Grid (Sidebar and Canvas) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Include Sidebar UserControl -->
            <uc:CommandSidebarView Grid.Column="0" />

            <!-- Include Canvas UserControl -->
            <uc:NetworkCanvasView Grid.Column="1"/>
        </Grid>
    </Grid>
</Window>